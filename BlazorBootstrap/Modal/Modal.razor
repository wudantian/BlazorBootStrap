
@if (isVisible)
{
    <div class="modal-backdrop fade show d-block"></div>
    <div class="modal fade show  d-block" tabindex="-1" role="dialog" aria-labelledby="modalTitle" aria-hidden="true" @onclick="Close2">
        <div class="modal-dialog modal-dialog-centered" role="document" @onmouseout="MouseOut" @onmouseover="MouseIn">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">@Title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close" @onclick="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                @Content
            </div>
        </div>
    </div>
}

@code {
    private bool isVisible = false;
    //private string ShowCss;

    [Parameter]
    public string Title { get; set; }

    [Parameter]
    public RenderFragment Content { get; set; }

    private bool active = false;
    private void MouseIn()
    {
        active = true;
    }

    private void MouseOut()
    {
        active = false;
    }
    public void Show()
    {
        isVisible = true;
    }

    public void Close()
    {
        isVisible = false;
        //ShowCss = "d-none";
        Title = null;
        Content = null;
    }

    private void Close2()
    {
        if (!active)
            Close();
    }
}
